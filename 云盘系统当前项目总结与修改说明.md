# 云盘系统当前项目总结与修改说明

> 更新时间：2026-08-31  
> 项目目录：`E:\distributed-cloud-disk`  
> 服务地址：`http://127.0.0.1:8080`  
> 默认测试账号：`alice / 123456`

## 1. 项目当前状态

当前项目已经形成一套可以在 Windows + Docker Desktop 环境运行的云盘系统，包含 Web 客户端、Qt 客户端、Nginx、FastCGI、MySQL、Redis 和 FastDFS。

主要访问链路如下：

```text
Web 浏览器 / Qt 客户端
          |
          v
Nginx（宿主机 8080 -> 容器 80）
          |
          v
7 个 FastCGI 接口
          |
          +---- MySQL：用户、Token、文件元数据
          +---- FastDFS：主要文件存储
          +---- 本地目录：FastDFS 不可用时的回退存储
```

当前 Docker 容器状态为 `Up`，以下 7 个 FastCGI 程序均配置并实际运行了 4 个工作进程：

| FastCGI 程序 | 端口 | 作用 | 工作进程数 |
|---|---:|---|---:|
| `register_fcgi` | 9000 | 用户注册 | 4 |
| `login_fcgi` | 9001 | 用户登录、签发 Token | 4 |
| `upload_fcgi` | 9999 | 文件上传 | 4 |
| `download_fcgi` | 9003 | 登录下载、分享下载 | 4 |
| `filelist_fcgi` | 9004 | 查询用户文件列表 | 4 |
| `delete_fcgi` | 9005 | 删除文件 | 4 |
| `share_fcgi` | 9006 | 创建分享链接 | 4 |

Redis 服务会随容器启动，但当前 Token 的签发和校验实际使用 MySQL，尚未接入 Redis。

## 2. 当前支持的功能

| 功能 | Web 客户端 | Qt 客户端 | 鉴权要求 | 当前状态 |
|---|---|---|---|---|
| 用户注册 | 支持 | 支持 | 无 | 可用 |
| 用户登录 | 支持 | 支持 | 无 | 可用 |
| 文件上传 | 支持 | 支持 | Token | 可用 |
| 文件列表 | 支持 | 支持 | Token | 可用 |
| 文件下载 | 支持 | 支持 | Token 或有效分享 Token | 可用 |
| 文件删除 | 支持 | 支持 | Token + 文件所有权 | 可用 |
| 文件分享 | 支持 | 支持 | Token + 文件所有权 | 可用 |
| 分享链接下载 | 支持 | 支持打开浏览器 | 有效且未过期的分享 Token | 可用 |

## 3. 本轮主要修改

### 3.1 Token 与接口鉴权

- 将原来可预测的 `srand()` / `rand()` Token 生成方式替换为安全随机字节生成方式。
- 登录成功后把 Token 保存到 MySQL，并通过 Token 反查真实用户 ID。
- 文件上传、列表、下载、删除和分享接口统一支持 `X-Auth-Token` 请求头。
- 为兼容已有测试方式，服务端仍保留查询参数 `token` 的读取方式；正式客户端优先使用请求头。
- 文件接口不再信任客户端随意提交的用户 ID，而是以 Token 对应的用户身份为准。
- Qt 客户端增加空 Token 防护，登录凭证异常时不再继续上传。

相关文件：

- `docker/server/fcgi_common.h`
- `docker/server/login_fcgi.c`
- `docker/server/upload_fcgi.c`
- `docker/server/filelist_fcgi.c`
- `docker/server/download_fcgi.c`
- `docker/server/delete_fcgi.c`
- `docker/server/share_fcgi.c`

### 3.2 上传功能

- 修复 Web 上传请求缺少 multipart boundary 导致的 `Missing multipart boundary`。
- 服务端按照 `Content-Type` 提取并校验 boundary。
- multipart 文件解析改为按实际字节长度处理，避免把二进制文件当作普通 C 字符串。
- 最大 POST 数据长度限制为 10 MiB，超出限制会拒绝处理。
- 服务端重新计算文件 MD5；客户端提供 MD5 时会进行一致性校验。
- 文件优先上传到 FastDFS；FastDFS 不可用时回退到容器持久化上传目录。
- 数据库记录新增 `storage_type` 和 `status`，用于区分存储类型和文件状态。
- Qt 上传请求补充 `X-Auth-Token`，修复 `Host requires authentication`。

### 3.3 下载功能

- 登录下载必须携带有效 Token，并校验文件是否属于当前用户。
- 分享下载通过 `share_token` 查询，不需要登录 Token。
- 分享 Token 必须存在、长度正确且没有过期。
- 支持从 FastDFS 或本地回退目录读取文件。
- 下载成功后更新 `download_count`。
- 下载响应使用文件名和二进制响应头，避免把文件内容作为 JSON 返回。

### 3.4 删除功能

- 删除前校验 Token 和文件所有权。
- 删除流程增加状态变化：`active -> deleting -> deleted/delete_failed`。
- 根据 `storage_type` 删除 FastDFS 文件或本地文件。
- 使用受影响行数检查，减少重复删除和并发状态异常。
- 文件列表只返回 `active` 状态的文件。

### 3.5 分享功能

- 分享前校验请求方法、JSON Content-Type、Token 和文件所有权。
- `expire_days` 只允许 1～365 天。
- 分享 Token 使用安全随机数生成，不再使用 `rand()`。
- 修复查询结果释放后继续使用文件名的内存生命周期问题。
- 分享成功后返回：

```json
{
  "code": "000",
  "data": {
    "share_url": "http://localhost:8080/download?share_token=...",
    "filename": "example.txt",
    "expire": "到期时间",
    "expire_days": 7
  }
}
```

- 分享基础地址通过 `SHARE_BASE_URL=http://localhost:8080` 配置。

### 3.6 输入、数据库和 JSON 处理

- 增加 POST 长度和整数范围校验。
- 对写入 SQL 的字符串进行转义。
- JSON 字符串输出统一进行特殊字符转义，避免文件名中的引号或反斜杠破坏响应。
- 增加 MySQL 查询结果为空、更新失败和受影响行数异常时的处理。
- 登录、注册、上传、下载、删除、分享等接口使用更明确的 HTTP 状态和错误码。

### 3.7 Docker 与启动流程

- 基础镜像改为：

```dockerfile
FROM m.daocloud.io/docker.io/library/ubuntu:22.04
```

- 使用项目内 `docker/sources/` 的压缩包构建 FastDFS、Nginx 模块和 spawn-fcgi，减少构建时对外部源码站的依赖。
- 增加 FastDFS 客户端库链接和头文件路径。
- FastDFS Tracker 地址在容器启动时根据容器实际 IP 写入配置，修复固定地址或回环地址不可用的问题。
- 增加以下持久化卷：
  - `cloud-data`：本地回退文件。
  - `cloud-mysql-data`：MySQL 数据。
  - `cloud-fastdfs-data`：FastDFS Storage 数据。
- MySQL 启动改为循环健康检查，不再只等待固定 3 秒。
- 启动时自动创建或迁移 `storage_type`、`status` 字段。
- 每个 FastCGI 接口由 1 个工作进程调整为 4 个工作进程。
- 删除 Compose 中已经废弃的 `version` 字段，消除 Docker Compose 警告。

### 3.8 Web 客户端

- 页面名称统一为“简单云盘”。
- 上传时显式设置 multipart boundary。
- 登录失效判断兼容 `401`、`INVALID_TOKEN` 和 `AUTH_REQUIRED`。
- 上传、文件列表、删除、分享遇到 Token 失效时会退出到登录页。
- 分享窗口读取 `data.share_url` 并显示完整链接及到期时间。

相关文件：

- `web/index.html`
- `docker/web/index.html`

### 3.9 Qt 客户端

当前使用的完整 Qt 客户端项目为：

```text
07 css-upload\04-代码\LoginDemo\LoginDemo.pro
```

已完成以下修改：

- 在 `LoginDemo.pro` 中补充 `mytitlebar.ui`，修复 `ui_mytitlebar.h: No such file or directory`。
- 将设置按钮槽函数改为与 UI 对象名一致的 `on_okBtn_clicked()`，修复设置页不能返回登录页。
- 默认服务器地址改为 `127.0.0.1:8080`。
- 旧默认地址 `192.168.226.128:80` 会迁移到本地 Docker 地址。
- 设置 IP 和端口后立即保存配置。
- 登录响应改为读取 `data.token` 和 `data.id`，不再读取错误的顶层字段。
- 删除硬编码用户 ID `2`，使用服务端返回的真实用户 ID。
- 上传请求增加 `X-Auth-Token`。
- 文件列表改为独立窗口，避免作为主窗口子控件被裁切。
- 文件列表默认大小改为 `1000 × 600`，最小大小为 `850 × 480`。
- 文件名列自适应剩余宽度，ID、大小和上传时间按内容调整，操作列固定为足够容纳三个按钮的宽度。
- 分享结果改为读取 `data.share_url` 和 `data.expire`。
- 分享链接为空或 JSON 格式错误时不再提示成功。
- 分享提示框显示可点击链接，同时将链接复制到剪贴板。

相关文件：

- `07 css-upload/04-代码/LoginDemo/LoginDemo.pro`
- `07 css-upload/04-代码/LoginDemo/login.cpp`
- `07 css-upload/04-代码/LoginDemo/login.h`
- `07 css-upload/04-代码/LoginDemo/logininstance.cpp`
- `07 css-upload/04-代码/LoginDemo/mainwindow.cpp`
- `07 css-upload/04-代码/LoginDemo/filelistwidget.cpp`

## 4. 当前验证结果

### 4.1 Qt 编译

验证环境：

```text
Qt：6.11.2 MinGW 64-bit
编译器：E:\QT\Tools\mingw1310_64
配置：Debug
结果：编译和链接成功
```

独立验证生成的程序：

```text
E:\qt-build\LoginDemo-Verify-Debug\debug\LoginDemo.exe
```

### 4.2 完整功能链路测试

2026-08-31 对当前运行容器执行了可清理的真实接口测试：

| 测试步骤 | 结果 |
|---|---|
| `alice` 登录 | `code=000` |
| 携带 Token 上传文件 | `code=000` |
| 文件列表包含新文件 | 通过 |
| 登录态下载 | HTTP 200 |
| 下载文件 MD5 与原文件一致 | 通过 |
| 创建 7 天分享链接 | `code=000` |
| 通过分享链接下载 | HTTP 200 |
| 删除测试文件 | `code=000` |
| 删除后不再出现在 active 文件列表 | 通过 |
| 测试数据清理 | 完成 |

### 4.3 本机轻量并发测试

测试对象：`GET /filelist`  
运行环境：本机 Docker Desktop，单容器，小文件列表  
请求数：500  
并发数：20

| 指标 | 结果 |
|---|---:|
| 成功请求 | 500 |
| 失败请求 | 0 |
| 总耗时 | 0.45 秒 |
| 本次测得吞吐量 | 1107.30 请求/秒 |
| 平均延迟 | 11.34 ms |
| P95 延迟 | 28.71 ms |
| 最大延迟 | 76.84 ms |

以上结果只代表当前电脑上的轻量只读接口测试，不代表公网、生产数据库、大文件上传或多机 FastDFS 环境的容量上限。

## 5. 运行方式

### 5.1 启动 Docker 服务

在 CMD 中执行：

```bat
cd /d E:\distributed-cloud-disk
docker compose -f ".\docker\docker-compose.yml" up -d
docker compose -f ".\docker\docker-compose.yml" ps
```

浏览器访问：

```text
http://localhost:8080
```

### 5.2 后端代码或 Dockerfile 修改后重新构建

```bat
cd /d E:\distributed-cloud-disk
docker compose -f ".\docker\docker-compose.yml" up -d --build --force-recreate
docker compose -f ".\docker\docker-compose.yml" ps
```

查看日志：

```bat
docker compose -f ".\docker\docker-compose.yml" logs --tail=200 cloud-disk
```

### 5.3 运行 Qt 客户端

1. 使用 Qt Creator 打开：

```text
E:\distributed-cloud-disk\07 css-upload\04-代码\LoginDemo\LoginDemo.pro
```

2. 选择构建套件：

```text
Desktop Qt 6.11.2 MinGW 64-bit
```

3. 按 `Ctrl+B` 编译。
4. 按 `Ctrl+R` 运行。
5. 登录参数：

```text
IP：127.0.0.1
端口：8080
用户名：alice
密码：123456
```

## 6. 当前仍需注意的问题

以下内容没有被本轮修改掩盖，仍应作为后续优化项：

1. 用户密码仍使用 MD5，不能满足正式系统的密码安全要求，建议改用 Argon2、bcrypt 或 scrypt。
2. 数据库连接信息仍写在 C 头文件中，正式部署应改为环境变量或 Secret。
3. 当前使用 HTTP，Token 和文件内容在公网部署时需要 HTTPS 保护。
4. Token 当前存储和校验在 MySQL 中，Redis 已启动但没有参与 Token 缓存、过期或撤销。
5. Qt 的 `config.json` 当前会保存用户 Token，正式版本不应长期明文保存访问凭证。
6. 单个上传请求上限为 10 MiB，尚不支持分片上传、断点续传和超大文件。
7. `SHARE_BASE_URL` 当前为 `http://localhost:8080`，部署到其他电脑或服务器时必须改为实际可访问域名或 IP。
8. 当前为单容器开发部署，MySQL、Nginx、FastDFS 和业务进程没有拆分成独立服务。
9. Qt 6 构建仍有 `globalPos()` 弃用提示和成员初始化顺序警告，不影响当前运行，但后续可以单独清理。
10. 现有 `docker/test*.sh` 多为调试阶段脚本，部分使用旧式查询参数 Token 或固定测试数据，建议后续合并成可重复、自动清理、带断言的正式测试套件。

## 7. 建议的后续顺序

1. 将密码哈希升级为 Argon2/bcrypt，并提供旧用户密码迁移方案。
2. 把数据库密码、分享基础地址等配置迁移到环境变量和 Secret。
3. 为 Token 增加明确的过期、刷新、退出撤销机制，并评估 Redis 缓存。
4. 增加 HTTPS 和反向代理安全配置。
5. 将现有调试脚本整理为自动化回归测试，并在每次修改后执行。
6. 分别对登录、列表、小文件上传、大文件上传和下载进行持续时间更长的压测。
7. 增加分片上传、断点续传、秒传和失败重试能力。

## 8. 结论

目前项目已经从“接口功能存在但文件处理、Token、安全校验和客户端适配不完整”的状态，推进到可以通过 Docker 启动、可以使用 Web 和 Qt 客户端完成登录、上传、列表、下载、删除、分享的可运行版本。

当前完整功能链路和轻量并发测试均已通过，但它仍属于本地开发/演示部署。要用于公网或正式环境，优先处理密码哈希、HTTPS、敏感配置、Token 生命周期和正式自动化测试。
